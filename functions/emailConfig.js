'use strict';

// The Gmail "Send As" alias all outreach/business email is sent from.
// Must already be verified as a Send As alias on the connected Gmail account,
// otherwise Gmail will silently fall back to the account's primary address.
const OUTREACH_FROM_ADDRESS = 'Dean Burt <dean@dean-da-dev.co.uk>';

module.exports = { OUTREACH_FROM_ADDRESS };
